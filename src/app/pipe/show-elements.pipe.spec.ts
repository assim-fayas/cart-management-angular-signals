import { ShowElementsPipe } from './show-elements.pipe';

describe('ShowElementsPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowElementsPipe();
    expect(pipe).toBeTruthy();
  });
});
